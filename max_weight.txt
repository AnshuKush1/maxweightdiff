int arr[]=[8,4,5,2,10];

int T,N,K;

int count=0;

while(arr++)
{
  
 for (int i=0;i<N;i++)
{
  arr[i]=arr[i+1];
  count++;
}
}

Arrays.sort(arr[]);

if(arr[]>(arr[]/2))
{  count++;
  k=count;
}

output=arr[N]-arr[k];

else
{
if(arr[]=arr[]/2)
arr[]=(arr[]/2 +1);
}
